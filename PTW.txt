Để khai báo một phần tử điều khiển khi nhấn vào sẽ gửi thông tin của form đi ta sử dụng thẻ: ~ <input type= “password” 
 Để khai báo một phần tử điều khiển tạo một nút nhấn trên trang web ta sử dụng thẻ: ~ <input type = “button”>
 Để khai báo một phần tử điều khiển để mở một hộp thoại giúp người dùng mở một file trên hệ thống thư mục của máy tính ta sử dụng thẻ: ~<input type= “hidden”
 Để xác định trình tự nhận tiêu điểm của phần tử thông qua bàn phím ta sử dụng thuộc tính: ~Id
 Thứ tự xép tầng của css theo độ ưu tiên từ thấp đến cao như sau: ~ Browser default, external style sheet, internal style sheet, inline style
 Css định nghĩa các style khác nhau cho cùng một loại html element bằng cách sử dụng: ~ Id selector
 Để liên kết tới external style sheet ta sử dụng thẻ: ~<link>
 Dreamweaver sử dụng giao thức gì để tải một site cục bộ lên web server?  ~ Fpt
 Dreamweaver cho phép làm việc với mấy cách trình bày tài liệu: ~4
 Tính năng nào trong dreamweaver cho phép hiển thị mã nguồn tài ...put type="text">
 Tag nào tạo ra 1 drop-down list? ~<list>
 Tag nào tạo ra 1 text area? ~<input type="textarea">
 Tag nào dùng để chèn 1 hình vào web? ~<image src="image.gif">
 Tag nào dùng để tạo hình nền cho web? ~<body background="background.gif">
 Css là viết tắt của? ~Cascading style sheets
 Muốn liên kết xhtml với 1 file định nghĩa css ta dùng dòng nào sau đây? ~<stylesheet>mystyle.css</stylesheet>
 Muốn liên kết xhtml với 1 file định nghĩa css ta dùng dòng nào sau đây? ~Tất cả các câu trên đều sai.
 Đặt dòng liên kết với file css ở vùng nào trong file xhtml? ~In the <body> section
 Tag nào định nghĩa css ở ngay trong file xhtml? ~<style>
 Thuộc tính nào định nghĩa css ngay trong 1 tag? ~Font
 Dòng nào tuân theo đúng cú pháp của css? ~Body:color=black
 Dòng nào thể hiện đúng một comment (lời chú thích) trong css? ~/* this is a comment */
 Dòng nào dùng để thay đổi màu nền? ~Background-color:
 Làm thế nào thêm màu nền cho tất cả các phần tử <h1> ? ~H1 {background-color:#ffffff}
 JavaScript là ngôn ngữ xử lý ở: ~Server/client
 Javascript là ngôn ngữ thông dịch hay biên dịch ~Biên dịch
 Phương thức viết chương trình của Javascript như thế nào? ~Viết chung với HTML
 Javascript là ngôn ngữ kịch bản có dấu được mã nguồn không? ~Hai phát biểu đều sai.
 JavaScript được bắt đầu bằng? ~<Javascript> …<Javascript>
 Javascript có các dạng biến? ~Tất cả các loại trên.
 Trong Javascript hàm parseInt() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Trong Javascript hàm parseFloat() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Lệnh prompt trong Javascript để làm gì? ~Hiện một thông báo nhập thông tin
 Trong Javascript sự kiện Onload thực hiện khi: ~Khi click chuột
 Trong Javascript sự kiện OnUnload thực hiện khi nào? ~Khi kết thúc một chương trình
 Trong Javascript sự kiện Onblur thực hiện khi nào? ~Khi di chuyển con chuột qua form.
 Trong Javascript sự kiện OnMouseOver thực hiện khi nào? ~Khi một đối tượng trong form có focus
 Trong Javascript sự kiện Onclick thự...trong JavaScript? ~ function myFunction()
 Hàm prompt(…) trong JavaScript dùng để làm gì? ~Hiển thị thông báo nhập thông tin
 Câu lệnh nào đúng thực hiện việc gọi một script từ bên ngoài có tên là xxx.js? ~undefined
 JavaScript là ngôn ngữ kịch bản có che dấu được mã nguồn không? ~Hai phát biểu trên đều sai
 Dùng cách nào có thể biết được trình duyệt đang được sử dụng tại máy client? ~undefined
 Sự kiện nào xảy ra khi người dùng kích vào một phần tử HTML trên trang? ~onmouseover
 Trong JavaScript sự kiện Onload xảy ra khi nào? ~Khi bắt đầu chạy chương trình
 Trong JavaScript sự kiện Onclick xảy ra khi nào? ~Khi một đối tượng trong form nhận focus
 Trong JavaScript sự kiện OnUnload xảy ra khi nào? ~ Khi di chuyển chuột qua
 Câu lệnh nào khai báo một biến trong JavaScript? ~ var carName;
 Ngôn ngữ JavaScript có phân biệt chữ hoa, chữ thường? ~undefined
 Cách viết câu lệnh IF nào sau đây là đúng? ~ if i = 5
 Cách nào để làm tròn 7.25 tới số nguyên gần nhất trong JavaScript? ~ rnd(7.25)
 JavaScript là ngôn ngữ xử lý ở: ~Server/client
 Javascript là ngôn ngữ thông dịch hay biên dịch ~Biên dịch
 Phương thức viết chương trình của Javascript như thế nào? ~Viết chung với HTML
 Javascript là ngôn ngữ kịch bản có dấu được mã nguồn không? ~Hai phát biểu đều sai.
 JavaScript được bắt đầu bằng? ~<Javascript> …<Javascript>
 Javascript có các dạng biến? ~Tất cả các loại trên.
 Trong Javascript hàm parseInt() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Trong Javascript hàm parseFloat() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Lệnh prompt trong Javascript để làm gì? ~Hiện một thông báo nhập thông tin
 Trong Javascript sự kiện Onload thực hiện khi: ~Khi click chuột
 Trong Javascript sự kiện OnUnload thực hiện khi nào? ~Khi kết thúc một chương trình
 Trong Javascript sự kiện Onblur thực hiện khi nào? ~Khi di chuyển con chuột qua form.
 Trong Javascript sự kiện OnMouseOver thực hiện khi nào? ~Khi một đối tượng trong form có focus
 Trong Javascript sự kiện Onclick thự...trong JavaScript? ~ function myFunction()
 Hàm prompt(…) trong JavaScript dùng để làm gì? ~Hiển thị thông báo nhập thông tin
 Câu lệnh nào đúng thực hiện việc gọi một script từ bên ngoài có tên là xxx.js? ~undefined
 JavaScript là ngôn ngữ kịch bản có che dấu được mã nguồn không? ~Hai phát biểu trên đều sai
 Dùng cách nào có thể biết được trình duyệt đang được sử dụng tại máy client? ~undefined
 Sự kiện nào xảy ra khi người dùng kích vào một phần tử HTML trên trang? ~onmouseover
 Trong JavaScript sự kiện Onload xảy ra khi nào? ~Khi bắt đầu chạy chương trình
 Trong JavaScript sự kiện Onclick xảy ra khi nào? ~Khi một đối tượng trong form nhận focus
 Trong JavaScript sự kiện OnUnload xảy ra khi nào? ~ Khi di chuyển chuột qua
 Câu lệnh nào khai báo một biến trong JavaScript? ~ var carName;
 Ngôn ngữ JavaScript có phân biệt chữ hoa, chữ thường? ~undefined
 Cách viết câu lệnh IF nào sau đây là đúng? ~ if i = 5
 Cách nào để làm tròn 7.25 tới số nguyên gần nhất trong JavaScript? ~ rnd(7.25)
 JavaScript là ngôn ngữ xử lý ở: ~Server/client
 Javascript là ngôn ngữ thông dịch hay biên dịch ~Biên dịch
 Phương thức viết chương trình của Javascript như thế nào? ~Viết chung với HTML
 Javascript là ngôn ngữ kịch bản có dấu được mã nguồn không? ~Hai phát biểu đều sai.
 JavaScript được bắt đầu bằng? ~<Javascript> …<Javascript>
 Javascript có các dạng biến? ~Tất cả các loại trên.
 Trong Javascript hàm parseInt() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Trong Javascript hàm parseFloat() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Lệnh prompt trong Javascript để làm gì? ~Hiện một thông báo nhập thông tin
 Trong Javascript sự kiện Onload thực hiện khi: ~Khi click chuột
 Trong Javascript sự kiện OnUnload thực hiện khi nào? ~Khi kết thúc một chương trình
 Trong Javascript sự kiện Onblur thực hiện khi nào? ~Khi di chuyển con chuột qua form.
 Trong Javascript sự kiện OnMouseOver thực hiện khi nào? ~Khi một đối tượng trong form có focus
 Trong Javascript sự kiện Onclick thự...trong JavaScript? ~ function myFunction()
 Hàm prompt(…) trong JavaScript dùng để làm gì? ~Hiển thị thông báo nhập thông tin
 Câu lệnh nào đúng thực hiện việc gọi một script từ bên ngoài có tên là xxx.js? ~undefined
 JavaScript là ngôn ngữ kịch bản có che dấu được mã nguồn không? ~Hai phát biểu trên đều sai
 Dùng cách nào có thể biết được trình duyệt đang được sử dụng tại máy client? ~undefined
 Sự kiện nào xảy ra khi người dùng kích vào một phần tử HTML trên trang? ~onmouseover
 Trong JavaScript sự kiện Onload xảy ra khi nào? ~Khi bắt đầu chạy chương trình
 Trong JavaScript sự kiện Onclick xảy ra khi nào? ~Khi một đối tượng trong form nhận focus
 Trong JavaScript sự kiện OnUnload xảy ra khi nào? ~ Khi di chuyển chuột qua
 Câu lệnh nào khai báo một biến trong JavaScript? ~ var carName;
 Ngôn ngữ JavaScript có phân biệt chữ hoa, chữ thường? ~undefined
 Cách viết câu lệnh IF nào sau đây là đúng? ~ if i = 5
 Cách nào để làm tròn 7.25 tới số nguyên gần nhất trong JavaScript? ~ rnd(7.25)
 Câu lệnh lặp For trong JavaScript có dạng nào sau đây? ~ for (biến = Điều kiện; Giá trị tăng; Giá trị cuối)
 Thực hiện kiểm tra nếu biến i không bằng 5, câu lệnh nào là đúng? ~ if (i <> 5)
  var a = [1, 2, 3];
var b = [1, 2, 3];
var c = '1,2,3';

console.log(a == c);
console.log(b == c);
console.log(a == b); ~true, false, false
  var a = [9];
var b = [10];

console.log(a == 9);
console.log(b == 10);
console.log(a < b); ~false false false
  let i = 0;

const arr = new Array(5);
arr.forEach(() => i++);

console.log(i); ~1
  function greatestNumberInArray(arr) {
  let greatest = 0;
  for (let i = 0; i < arr.length; i++) {
    if (greatest < arr[i]) {
      greatest = arr[i];
    }
  }
  return greatest;
} ~undefined
  const arr = [1, 2, 3];

const a = arr.reduce(
  (acc, el, i) => ({ ...acc, [el]: i }),
  {}
);

const b = {};
for (let i = 0; i < arr.length; i++) {
  b[arr[i]] = i;
} ~undefined
  const arr = [
  x => x * 1,
  x => x * 2,
  x => x * 3,
  x => x * 4
];

console.log(arr.red...ệu HTML ta dùng thẻ: ~<BLOCKQUOTE> 
 Để nhóm các thành phần có liên quan với nhau ta dùng thẻ: ~<pre>
 Để khai báo một danh sách có thứ tự ta sử dụng thẻ: ~<lo>
 Để xác định kiểu chữ, kích thước, màu sắc... Ta dùng thẻ: ~ <font>
 Để khai báo một bảng trên trang web ta sử dụng thẻ: ~<th>
 Để khai báo một hàng trong bảng trên trang web ta sử dụng thẻ:  ~<table>
 Để tạo ra những ô mà chúng có thể kéo rộng ra hơn một dòng trên bảng ta sử dụng thuộc tính: ~Valign
 Để canh lề dọc cho các ô trong bảng ta sử dụng thuộc tính: ~Valign
 Để định nghĩa một tập các frame đơn ta sử dụng thẻ:  ~<frame>
 Để khai báo một phần tử điều khiển nhập văn bản chỉ có một dòng ta sử dụng thẻ:  ~<input type= “password”
 Để khai báo một phần tử điều khiển ẩn có chứa một value để phục vụ cho các mục đích khác trên trang web mà không muốn hiển thị ra ta dùng thẻ: ~<input type = “hidden”
 Để khai báo một phần tử điều khiển cho phép người dùng có thể chọn một hay nhiều giá trị ta sử dụng thẻ: ~<input type = “radio”
 Câu lệnh lặp For trong JavaScript có dạng nào sau đây? ~ for (biến = Điều kiện; Giá trị tăng; Giá trị cuối)
 Thực hiện kiểm tra nếu biến i không bằng 5, câu lệnh nào là đúng? ~ if (i <> 5)
  var a = [1, 2, 3];
var b = [1, 2, 3];
var c = '1,2,3';

console.log(a == c);
console.log(b == c);
console.log(a == b); ~true, false, false
  var a = [9];
var b = [10];

console.log(a == 9);
console.log(b == 10);
console.log(a < b); ~false false false
  let i = 0;

const arr = new Array(5);
arr.forEach(() => i++);

console.log(i); ~1
  function greatestNumberInArray(arr) {
  let greatest = 0;
  for (let i = 0; i < arr.length; i++) {
    if (greatest < arr[i]) {
      greatest = arr[i];
    }
  }
  return greatest;
} ~undefined
  const arr = [1, 2, 3];

const a = arr.reduce(
  (acc, el, i) => ({ ...acc, [el]: i }),
  {}
);

const b = {};
for (let i = 0; i < arr.length; i++) {
  b[arr[i]] = i;
} ~undefined
  const arr = [
  x => x * 1,
  x => x * 2,
  x => x * 3,
  x => x * 4
];

console.log(arr.red...ệu HTML ta dùng thẻ: ~<BLOCKQUOTE> 
 Để nhóm các thành phần có liên quan với nhau ta dùng thẻ: ~<pre>
 Để khai báo một danh sách có thứ tự ta sử dụng thẻ: ~<lo>
 Để xác định kiểu chữ, kích thước, màu sắc... Ta dùng thẻ: ~ <font>
 Để khai báo một bảng trên trang web ta sử dụng thẻ: ~<th>
 Để khai báo một hàng trong bảng trên trang web ta sử dụng thẻ:  ~<table>
 Để tạo ra những ô mà chúng có thể kéo rộng ra hơn một dòng trên bảng ta sử dụng thuộc tính: ~Valign
 Để canh lề dọc cho các ô trong bảng ta sử dụng thuộc tính: ~Valign
 Để định nghĩa một tập các frame đơn ta sử dụng thẻ:  ~<frame>
 Để khai báo một phần tử điều khiển nhập văn bản chỉ có một dòng ta sử dụng thẻ:  ~<input type= “password”
 Để khai báo một phần tử điều khiển ẩn có chứa một value để phục vụ cho các mục đích khác trên trang web mà không muốn hiển thị ra ta dùng thẻ: ~<input type = “hidden”
 Để khai báo một phần tử điều khiển cho phép người dùng có thể chọn một hay nhiều giá trị ta sử dụng thẻ: ~<input type = “radio”
 Câu lệnh lặp For trong JavaScript có dạng nào sau đây? ~ for (biến = Điều kiện; Giá trị tăng; Giá trị cuối)
 Thực hiện kiểm tra nếu biến i không bằng 5, câu lệnh nào là đúng? ~ if (i <> 5)
  var a = [1, 2, 3];
var b = [1, 2, 3];
var c = '1,2,3';

console.log(a == c);
console.log(b == c);
console.log(a == b); ~true, false, false
  var a = [9];
var b = [10];

console.log(a == 9);
console.log(b == 10);
console.log(a < b); ~false false false
  let i = 0;

const arr = new Array(5);
arr.forEach(() => i++);

console.log(i); ~1
  function greatestNumberInArray(arr) {
  let greatest = 0;
  for (let i = 0; i < arr.length; i++) {
    if (greatest < arr[i]) {
      greatest = arr[i];
    }
  }
  return greatest;
} ~undefined
  const arr = [1, 2, 3];

const a = arr.reduce(
  (acc, el, i) => ({ ...acc, [el]: i }),
  {}
);

const b = {};
for (let i = 0; i < arr.length; i++) {
  b[arr[i]] = i;
} ~undefined
  const arr = [
  x => x * 1,
  x => x * 2,
  x => x * 3,
  x => x * 4
];

console.log(arr.red...ệu HTML ta dùng thẻ: ~<BLOCKQUOTE> 
 Để nhóm các thành phần có liên quan với nhau ta dùng thẻ: ~<pre>
 Để khai báo một danh sách có thứ tự ta sử dụng thẻ: ~<lo>
 Để xác định kiểu chữ, kích thước, màu sắc... Ta dùng thẻ: ~ <font>
 Để khai báo một bảng trên trang web ta sử dụng thẻ: ~<th>
 Để khai báo một hàng trong bảng trên trang web ta sử dụng thẻ:  ~<table>
 Để tạo ra những ô mà chúng có thể kéo rộng ra hơn một dòng trên bảng ta sử dụng thuộc tính: ~Valign
 Để canh lề dọc cho các ô trong bảng ta sử dụng thuộc tính: ~Valign
 Để định nghĩa một tập các frame đơn ta sử dụng thẻ:  ~<frame>
 Để khai báo một phần tử điều khiển nhập văn bản chỉ có một dòng ta sử dụng thẻ:  ~<input type= “password”
 Để khai báo một phần tử điều khiển ẩn có chứa một value để phục vụ cho các mục đích khác trên trang web mà không muốn hiển thị ra ta dùng thẻ: ~<input type = “hidden”
 Để khai báo một phần tử điều khiển cho phép người dùng có thể chọn một hay nhiều giá trị ta sử dụng thẻ: ~<input type = “radio”
 Để khai báo một phần tử điều khiển khi nhấn vào sẽ gửi thông tin của form đi ta sử dụng thẻ: ~ <input type= “text”>
 Để khai báo một phần tử điều khiển tạo một nút nhấn trên trang web ta sử dụng thẻ: ~ <input type = “button”>
 Để khai báo một phần tử điều khiển để mở một hộp thoại giúp người dùng mở một file trên hệ thống thư mục của máy tính ta sử dụng thẻ: ~ <input type = “button”>
 Để xác định trình tự nhận tiêu điểm của phần tử thông qua bàn phím ta sử dụng thuộc tính: ~ Focus
 Thứ tự xép tầng của css theo độ ưu tiên từ thấp đến cao như sau: ~Browser default, internal style sheet, inline style, external style sheet 
 Css định nghĩa các style khác nhau cho cùng một loại html element bằng cách sử dụng: ~Class selector
 Để liên kết tới external style sheet ta sử dụng thẻ: ~<a>
 Dreamweaver sử dụng giao thức gì để tải một site cục bộ lên web server?  ~ Fpt
 Dreamweaver cho phép làm việc với mấy cách trình bày tài liệu: ~3
 Tính năng nào trong dreamweaver cho phép hiển thị mã nguồn tà... text input field? ~<textfield>
 Tag nào tạo ra 1 drop-down list? ~<list>
 Tag nào tạo ra 1 text area? ~<textarea>
 Tag nào dùng để chèn 1 hình vào web? ~<img href="image.gif>
 Tag nào dùng để tạo hình nền cho web? ~undefined
 Css là viết tắt của? ~Colorful style sheets
 Muốn liên kết xhtml với 1 file định nghĩa css ta dùng dòng nào sau đây? ~<stylesheet>mystyle.css</stylesheet>
 Muốn liên kết xhtml với 1 file định nghĩa css ta dùng dòng nào sau đây? ~<stylesheet>mystyle.css</stylesheet>
 Đặt dòng liên kết với file css ở vùng nào trong file xhtml? ~At the top of the document
 Tag nào định nghĩa css ở ngay trong file xhtml? ~<script>
 Thuộc tính nào định nghĩa css ngay trong 1 tag? ~Style
 Dòng nào tuân theo đúng cú pháp của css? ~{body:color=black(body}
 Dòng nào thể hiện đúng một comment (lời chú thích) trong css? ~/* this is a comment */
 Dòng nào dùng để thay đổi màu nền? ~Background-color:
 Làm thế nào thêm màu nền cho tất cả các phần tử <h1> ? ~All.h1 {background-color:#ffffff}
 Làm thế nào thay màu nền của chữ (text)? ~Color:
 Thuộc tính nào thay đổi kích cỡ chữ? ~Font-style
 Thuộc tính nào làm chữ trong tag p trở thành chữ đậm? ~P {font-weight:bold}
 Làm sao để hiển thị liên kết mà ko có gạch chân bên dưới? ~A {text-decoration:no underline}
 Làm sao để mỗi từ trong 1 dòng đều viết hoa ở đầu từ? ~Text-transform:uppercase
 Làm sao để thay đổi font của mỗi phần tử? ~F:
 Làm sao để tạo chữ đậm? ~Tất cả các câu trên đều đúng
 Làm thế nào để hiển thị viền 1 phần tử với kích thước đường viền như sau: ~Border-width:10px 1px 5px 20px
 Làm sao để thay đổi lề trái của một phần tử? ~Margin-left:
 Để định nghĩa khoảng trống giữa các cạnh (viền) của phần tử và nội dung, bạn sử dụng thuộc tính padding, có thể gán giá trị âm cho thuộc tính này không? ~undefined
 Làm thế nào để hình ở đầu mỗi dòng của 1 list (danh sách) có hình vuông? ~List-type: square
 Nút lệnh(button) nào.dưới đây cho phép gửi thông tin trên form tới server khi được click Chuột? ~Send
 Câu lệnh nào cho ... External Style sheet ta sử dụng thẻ: ~<LINK>
  Dreamweaver cho phép làm việc với mấy cách trình bày tài liệu: ~4
 Làm thế nào để bo tròn góc cạnh các phần tử? ~border-radius: 30px;
 Làm thế nào để thêm bóng (shadow) cho các phần tử trong CSS3? ~shadow-right: 10px shadow-bottom: 10px; 
 Làm thế nào để thêm hình ảnh xuất hiện trên đường viền cho các phần tử trong CSS3? ~border-variable: image url(image.png);
 Làm cách nào để thay đổi kích thước hình ảnh background bằng CSS3? ~background-proportion: 80px 60px;
  Làm cách nào để thêm bóng (shadow) cho đoạn text bằng CSS3? ~text-shadow: 5px 5px 5px grey;
 Làm thế nào để bắt buộc đoạn text xuống dòng? ~text-wrap: break-word;
 Đâu là lệnh chuyển đổi (transform) hợp lệ trong CSS3. ~matrix() 
 Làm thế nào để xoay phần tử trong CSS3? ~transform: rotate(30deg);
 Đâu là cách để chuyển đổi kích thước/tỷ lệ phần tử bằng CSS3? ~scale-object: 2,4;
 Làm cách nào để tạo hiệu ứng chuyển tiếp transition bằng CSS3? ~alpha-effect: transition (width,2s);
 Để khai báo một phần tử điều khiển khi nhấn vào sẽ gửi thông tin của form đi ta sử dụng thẻ: ~ <input type= “password” 
 Để khai báo một phần tử điều khiển tạo một nút nhấn trên trang web ta sử dụng thẻ: ~ <input type = “button”>
 Để khai báo một phần tử điều khiển để mở một hộp thoại giúp người dùng mở một file trên hệ thống thư mục của máy tính ta sử dụng thẻ: ~<input type= “hidden”
 Để xác định trình tự nhận tiêu điểm của phần tử thông qua bàn phím ta sử dụng thuộc tính: ~Id
 Thứ tự xép tầng của css theo độ ưu tiên từ thấp đến cao như sau: ~ Browser default, external style sheet, internal style sheet, inline style
 Css định nghĩa các style khác nhau cho cùng một loại html element bằng cách sử dụng: ~ Id selector
 Để liên kết tới external style sheet ta sử dụng thẻ: ~<link>
 Dreamweaver sử dụng giao thức gì để tải một site cục bộ lên web server?  ~ Fpt
 Dreamweaver cho phép làm việc với mấy cách trình bày tài liệu: ~4
 Tính năng nào trong dreamweaver cho phép hiển thị mã nguồn tài ...put type="text">
 Tag nào tạo ra 1 drop-down list? ~<list>
 Tag nào tạo ra 1 text area? ~<input type="textarea">
 Tag nào dùng để chèn 1 hình vào web? ~<image src="image.gif">
 Tag nào dùng để tạo hình nền cho web? ~<body background="background.gif">
 Css là viết tắt của? ~Cascading style sheets
 Muốn liên kết xhtml với 1 file định nghĩa css ta dùng dòng nào sau đây? ~<stylesheet>mystyle.css</stylesheet>
 Muốn liên kết xhtml với 1 file định nghĩa css ta dùng dòng nào sau đây? ~Tất cả các câu trên đều sai.
 Đặt dòng liên kết với file css ở vùng nào trong file xhtml? ~In the <body> section
 Tag nào định nghĩa css ở ngay trong file xhtml? ~<style>
 Thuộc tính nào định nghĩa css ngay trong 1 tag? ~Font
 Dòng nào tuân theo đúng cú pháp của css? ~Body:color=black
 Dòng nào thể hiện đúng một comment (lời chú thích) trong css? ~/* this is a comment */
 Dòng nào dùng để thay đổi màu nền? ~Background-color:
 Làm thế nào thêm màu nền cho tất cả các phần tử <h1> ? ~H1 {background-color:#ffffff}
 JavaScript là ngôn ngữ xử lý ở: ~Server/client
 Javascript là ngôn ngữ thông dịch hay biên dịch ~Biên dịch
 Phương thức viết chương trình của Javascript như thế nào? ~Viết chung với HTML
 Javascript là ngôn ngữ kịch bản có dấu được mã nguồn không? ~Hai phát biểu đều sai.
 JavaScript được bắt đầu bằng? ~<Javascript> …<Javascript>
 Javascript có các dạng biến? ~Tất cả các loại trên.
 Trong Javascript hàm parseInt() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Trong Javascript hàm parseFloat() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Lệnh prompt trong Javascript để làm gì? ~Hiện một thông báo nhập thông tin
 Trong Javascript sự kiện Onload thực hiện khi: ~Khi click chuột
 Trong Javascript sự kiện OnUnload thực hiện khi nào? ~Khi kết thúc một chương trình
 Trong Javascript sự kiện Onblur thực hiện khi nào? ~Khi di chuyển con chuột qua form.
 Trong Javascript sự kiện OnMouseOver thực hiện khi nào? ~Khi một đối tượng trong form có focus
 Trong Javascript sự kiện Onclick thự...trong JavaScript? ~ function myFunction()
 Hàm prompt(…) trong JavaScript dùng để làm gì? ~Hiển thị thông báo nhập thông tin
 Câu lệnh nào đúng thực hiện việc gọi một script từ bên ngoài có tên là xxx.js? ~undefined
 JavaScript là ngôn ngữ kịch bản có che dấu được mã nguồn không? ~Hai phát biểu trên đều sai
 Dùng cách nào có thể biết được trình duyệt đang được sử dụng tại máy client? ~undefined
 Sự kiện nào xảy ra khi người dùng kích vào một phần tử HTML trên trang? ~onmouseover
 Trong JavaScript sự kiện Onload xảy ra khi nào? ~Khi bắt đầu chạy chương trình
 Trong JavaScript sự kiện Onclick xảy ra khi nào? ~Khi một đối tượng trong form nhận focus
 Trong JavaScript sự kiện OnUnload xảy ra khi nào? ~ Khi di chuyển chuột qua
 Câu lệnh nào khai báo một biến trong JavaScript? ~ var carName;
 Ngôn ngữ JavaScript có phân biệt chữ hoa, chữ thường? ~undefined
 Cách viết câu lệnh IF nào sau đây là đúng? ~ if i = 5
 Cách nào để làm tròn 7.25 tới số nguyên gần nhất trong JavaScript? ~ rnd(7.25)
 JavaScript là ngôn ngữ xử lý ở: ~Server/client
 Javascript là ngôn ngữ thông dịch hay biên dịch ~Biên dịch
 Phương thức viết chương trình của Javascript như thế nào? ~Viết chung với HTML
 Javascript là ngôn ngữ kịch bản có dấu được mã nguồn không? ~Hai phát biểu đều sai.
 JavaScript được bắt đầu bằng? ~<Javascript> …<Javascript>
 Javascript có các dạng biến? ~Tất cả các loại trên.
 Trong Javascript hàm parseInt() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Trong Javascript hàm parseFloat() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Lệnh prompt trong Javascript để làm gì? ~Hiện một thông báo nhập thông tin
 Trong Javascript sự kiện Onload thực hiện khi: ~Khi click chuột
 Trong Javascript sự kiện OnUnload thực hiện khi nào? ~Khi kết thúc một chương trình
 Trong Javascript sự kiện Onblur thực hiện khi nào? ~Khi di chuyển con chuột qua form.
 Trong Javascript sự kiện OnMouseOver thực hiện khi nào? ~Khi một đối tượng trong form có focus
 Trong Javascript sự kiện Onclick thự...trong JavaScript? ~ function myFunction()
 Hàm prompt(…) trong JavaScript dùng để làm gì? ~Hiển thị thông báo nhập thông tin
 Câu lệnh nào đúng thực hiện việc gọi một script từ bên ngoài có tên là xxx.js? ~undefined
 JavaScript là ngôn ngữ kịch bản có che dấu được mã nguồn không? ~Hai phát biểu trên đều sai
 Dùng cách nào có thể biết được trình duyệt đang được sử dụng tại máy client? ~undefined
 Sự kiện nào xảy ra khi người dùng kích vào một phần tử HTML trên trang? ~onmouseover
 Trong JavaScript sự kiện Onload xảy ra khi nào? ~Khi bắt đầu chạy chương trình
 Trong JavaScript sự kiện Onclick xảy ra khi nào? ~Khi một đối tượng trong form nhận focus
 Trong JavaScript sự kiện OnUnload xảy ra khi nào? ~ Khi di chuyển chuột qua
 Câu lệnh nào khai báo một biến trong JavaScript? ~ var carName;
 Ngôn ngữ JavaScript có phân biệt chữ hoa, chữ thường? ~undefined
 Cách viết câu lệnh IF nào sau đây là đúng? ~ if i = 5
 Cách nào để làm tròn 7.25 tới số nguyên gần nhất trong JavaScript? ~ rnd(7.25)
 JavaScript là ngôn ngữ xử lý ở: ~Server/client
 Javascript là ngôn ngữ thông dịch hay biên dịch ~Biên dịch
 Phương thức viết chương trình của Javascript như thế nào? ~Viết chung với HTML
 Javascript là ngôn ngữ kịch bản có dấu được mã nguồn không? ~Hai phát biểu đều sai.
 JavaScript được bắt đầu bằng? ~<Javascript> …<Javascript>
 Javascript có các dạng biến? ~Tất cả các loại trên.
 Trong Javascript hàm parseInt() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Trong Javascript hàm parseFloat() dùng để làm gì? ~Chuyển một chuỗi thành số nguyên
 Lệnh prompt trong Javascript để làm gì? ~Hiện một thông báo nhập thông tin
 Trong Javascript sự kiện Onload thực hiện khi: ~Khi click chuột
 Trong Javascript sự kiện OnUnload thực hiện khi nào? ~Khi kết thúc một chương trình
 Trong Javascript sự kiện Onblur thực hiện khi nào? ~Khi di chuyển con chuột qua form.
 Trong Javascript sự kiện OnMouseOver thực hiện khi nào? ~Khi một đối tượng trong form có focus
 Trong Javascript sự kiện Onclick thự...trong JavaScript? ~ function myFunction()
 Hàm prompt(…) trong JavaScript dùng để làm gì? ~Hiển thị thông báo nhập thông tin
 Câu lệnh nào đúng thực hiện việc gọi một script từ bên ngoài có tên là xxx.js? ~undefined
 JavaScript là ngôn ngữ kịch bản có che dấu được mã nguồn không? ~Hai phát biểu trên đều sai
 Dùng cách nào có thể biết được trình duyệt đang được sử dụng tại máy client? ~undefined
 Sự kiện nào xảy ra khi người dùng kích vào một phần tử HTML trên trang? ~onmouseover
 Trong JavaScript sự kiện Onload xảy ra khi nào? ~Khi bắt đầu chạy chương trình
 Trong JavaScript sự kiện Onclick xảy ra khi nào? ~Khi một đối tượng trong form nhận focus
 Trong JavaScript sự kiện OnUnload xảy ra khi nào? ~ Khi di chuyển chuột qua
 Câu lệnh nào khai báo một biến trong JavaScript? ~ var carName;
 Ngôn ngữ JavaScript có phân biệt chữ hoa, chữ thường? ~undefined
 Cách viết câu lệnh IF nào sau đây là đúng? ~ if i = 5
 Cách nào để làm tròn 7.25 tới số nguyên gần nhất trong JavaScript? ~ rnd(7.25)
 Câu lệnh lặp For trong JavaScript có dạng nào sau đây? ~ for (biến = Điều kiện; Giá trị tăng; Giá trị cuối)
 Thực hiện kiểm tra nếu biến i không bằng 5, câu lệnh nào là đúng? ~ if (i <> 5)
  var a = [1, 2, 3];
var b = [1, 2, 3];
var c = '1,2,3';

console.log(a == c);
console.log(b == c);
console.log(a == b); ~true, false, false
  var a = [9];
var b = [10];

console.log(a == 9);
console.log(b == 10);
console.log(a < b); ~false false false
  let i = 0;

const arr = new Array(5);
arr.forEach(() => i++);

console.log(i); ~1
  function greatestNumberInArray(arr) {
  let greatest = 0;
  for (let i = 0; i < arr.length; i++) {
    if (greatest < arr[i]) {
      greatest = arr[i];
    }
  }
  return greatest;
} ~undefined
  const arr = [1, 2, 3];

const a = arr.reduce(
  (acc, el, i) => ({ ...acc, [el]: i }),
  {}
);

const b = {};
for (let i = 0; i < arr.length; i++) {
  b[arr[i]] = i;
} ~undefined
  const arr = [
  x => x * 1,
  x => x * 2,
  x => x * 3,
  x => x * 4
];

console.log(arr.red...ệu HTML ta dùng thẻ: ~<BLOCKQUOTE> 
 Để nhóm các thành phần có liên quan với nhau ta dùng thẻ: ~<pre>
 Để khai báo một danh sách có thứ tự ta sử dụng thẻ: ~<lo>
 Để xác định kiểu chữ, kích thước, màu sắc... Ta dùng thẻ: ~ <font>
 Để khai báo một bảng trên trang web ta sử dụng thẻ: ~<th>
 Để khai báo một hàng trong bảng trên trang web ta sử dụng thẻ:  ~<table>
 Để tạo ra những ô mà chúng có thể kéo rộng ra hơn một dòng trên bảng ta sử dụng thuộc tính: ~Valign
 Để canh lề dọc cho các ô trong bảng ta sử dụng thuộc tính: ~Valign
 Để định nghĩa một tập các frame đơn ta sử dụng thẻ:  ~<frame>
 Để khai báo một phần tử điều khiển nhập văn bản chỉ có một dòng ta sử dụng thẻ:  ~<input type= “password”
 Để khai báo một phần tử điều khiển ẩn có chứa một value để phục vụ cho các mục đích khác trên trang web mà không muốn hiển thị ra ta dùng thẻ: ~<input type = “hidden”
 Để khai báo một phần tử điều khiển cho phép người dùng có thể chọn một hay nhiều giá trị ta sử dụng thẻ: ~<input type = “radio”
 Câu lệnh lặp For trong JavaScript có dạng nào sau đây? ~ for (biến = Điều kiện; Giá trị tăng; Giá trị cuối)
 Thực hiện kiểm tra nếu biến i không bằng 5, câu lệnh nào là đúng? ~ if (i <> 5)
  var a = [1, 2, 3];
var b = [1, 2, 3];
var c = '1,2,3';

console.log(a == c);
console.log(b == c);
console.log(a == b); ~true, false, false
  var a = [9];
var b = [10];

console.log(a == 9);
console.log(b == 10);
console.log(a < b); ~false false false
  let i = 0;

const arr = new Array(5);
arr.forEach(() => i++);

console.log(i); ~1
  function greatestNumberInArray(arr) {
  let greatest = 0;
  for (let i = 0; i < arr.length; i++) {
    if (greatest < arr[i]) {
      greatest = arr[i];
    }
  }
  return greatest;
} ~undefined
  const arr = [1, 2, 3];

const a = arr.reduce(
  (acc, el, i) => ({ ...acc, [el]: i }),
  {}
);

const b = {};
for (let i = 0; i < arr.length; i++) {
  b[arr[i]] = i;
} ~undefined
  const arr = [
  x => x * 1,
  x => x * 2,
  x => x * 3,
  x => x * 4
];

console.log(arr.red...ệu HTML ta dùng thẻ: ~<BLOCKQUOTE> 
 Để nhóm các thành phần có liên quan với nhau ta dùng thẻ: ~<pre>
 Để khai báo một danh sách có thứ tự ta sử dụng thẻ: ~<lo>
 Để xác định kiểu chữ, kích thước, màu sắc... Ta dùng thẻ: ~ <font>
 Để khai báo một bảng trên trang web ta sử dụng thẻ: ~<th>
 Để khai báo một hàng trong bảng trên trang web ta sử dụng thẻ:  ~<table>
 Để tạo ra những ô mà chúng có thể kéo rộng ra hơn một dòng trên bảng ta sử dụng thuộc tính: ~Valign
 Để canh lề dọc cho các ô trong bảng ta sử dụng thuộc tính: ~Valign
 Để định nghĩa một tập các frame đơn ta sử dụng thẻ:  ~<frame>
 Để khai báo một phần tử điều khiển nhập văn bản chỉ có một dòng ta sử dụng thẻ:  ~<input type= “password”
 Để khai báo một phần tử điều khiển ẩn có chứa một value để phục vụ cho các mục đích khác trên trang web mà không muốn hiển thị ra ta dùng thẻ: ~<input type = “hidden”
 Để khai báo một phần tử điều khiển cho phép người dùng có thể chọn một hay nhiều giá trị ta sử dụng thẻ: ~<input type = “radio”
 Câu lệnh lặp For trong JavaScript có dạng nào sau đây? ~ for (biến = Điều kiện; Giá trị tăng; Giá trị cuối)
 Thực hiện kiểm tra nếu biến i không bằng 5, câu lệnh nào là đúng? ~ if (i <> 5)
  var a = [1, 2, 3];
var b = [1, 2, 3];
var c = '1,2,3';

console.log(a == c);
console.log(b == c);
console.log(a == b); ~true, false, false
  var a = [9];
var b = [10];

console.log(a == 9);
console.log(b == 10);
console.log(a < b); ~false false false
  let i = 0;

const arr = new Array(5);
arr.forEach(() => i++);

console.log(i); ~1
  function greatestNumberInArray(arr) {
  let greatest = 0;
  for (let i = 0; i < arr.length; i++) {
    if (greatest < arr[i]) {
      greatest = arr[i];
    }
  }
  return greatest;
} ~undefined
  const arr = [1, 2, 3];

const a = arr.reduce(
  (acc, el, i) => ({ ...acc, [el]: i }),
  {}
);

const b = {};
for (let i = 0; i < arr.length; i++) {
  b[arr[i]] = i;
} ~undefined
  const arr = [
  x => x * 1,
  x => x * 2,
  x => x * 3,
  x => x * 4
];

console.log(arr.red...ệu HTML ta dùng thẻ: ~<BLOCKQUOTE> 
 Để nhóm các thành phần có liên quan với nhau ta dùng thẻ: ~<pre>
 Để khai báo một danh sách có thứ tự ta sử dụng thẻ: ~<lo>
 Để xác định kiểu chữ, kích thước, màu sắc... Ta dùng thẻ: ~ <font>
 Để khai báo một bảng trên trang web ta sử dụng thẻ: ~<th>
 Để khai báo một hàng trong bảng trên trang web ta sử dụng thẻ:  ~<table>
 Để tạo ra những ô mà chúng có thể kéo rộng ra hơn một dòng trên bảng ta sử dụng thuộc tính: ~Valign
 Để canh lề dọc cho các ô trong bảng ta sử dụng thuộc tính: ~Valign
 Để định nghĩa một tập các frame đơn ta sử dụng thẻ:  ~<frame>
 Để khai báo một phần tử điều khiển nhập văn bản chỉ có một dòng ta sử dụng thẻ:  ~<input type= “password”
 Để khai báo một phần tử điều khiển ẩn có chứa một value để phục vụ cho các mục đích khác trên trang web mà không muốn hiển thị ra ta dùng thẻ: ~<input type = “hidden”
 Để khai báo một phần tử điều khiển cho phép người dùng có thể chọn một hay nhiều giá trị ta sử dụng thẻ: ~<input type = “radio”
 Để khai báo một phần tử điều khiển khi nhấn vào sẽ gửi thông tin của form đi ta sử dụng thẻ: ~ <input type= “text”>
 Để khai báo một phần tử điều khiển tạo một nút nhấn trên trang web ta sử dụng thẻ: ~ <input type = “button”>
 Để khai báo một phần tử điều khiển để mở một hộp thoại giúp người dùng mở một file trên hệ thống thư mục của máy tính ta sử dụng thẻ: ~ <input type = “button”>
 Để xác định trình tự nhận tiêu điểm của phần tử thông qua bàn phím ta sử dụng thuộc tính: ~ Focus
 Thứ tự xép tầng của css theo độ ưu tiên từ thấp đến cao như sau: ~Browser default, internal style sheet, inline style, external style sheet 
 Css định nghĩa các style khác nhau cho cùng một loại html element bằng cách sử dụng: ~Class selector
 Để liên kết tới external style sheet ta sử dụng thẻ: ~<a>
 Dreamweaver sử dụng giao thức gì để tải một site cục bộ lên web server?  ~ Fpt
 Dreamweaver cho phép làm việc với mấy cách trình bày tài liệu: ~3
 Tính năng nào trong dreamweaver cho phép hiển thị mã nguồn tà... text input field? ~<textfield>
 Tag nào tạo ra 1 drop-down list? ~<list>
 Tag nào tạo ra 1 text area? ~<textarea>
 Tag nào dùng để chèn 1 hình vào web? ~<img href="image.gif>
 Tag nào dùng để tạo hình nền cho web? ~undefined
 Css là viết tắt của? ~Colorful style sheets
 Muốn liên kết xhtml với 1 file định nghĩa css ta dùng dòng nào sau đây? ~<stylesheet>mystyle.css</stylesheet>
 Muốn liên kết xhtml với 1 file định nghĩa css ta dùng dòng nào sau đây? ~<stylesheet>mystyle.css</stylesheet>
 Đặt dòng liên kết với file css ở vùng nào trong file xhtml? ~At the top of the document
 Tag nào định nghĩa css ở ngay trong file xhtml? ~<script>
 Thuộc tính nào định nghĩa css ngay trong 1 tag? ~Style
 Dòng nào tuân theo đúng cú pháp của css? ~{body:color=black(body}
 Dòng nào thể hiện đúng một comment (lời chú thích) trong css? ~/* this is a comment */
 Dòng nào dùng để thay đổi màu nền? ~Background-color:
 Làm thế nào thêm màu nền cho tất cả các phần tử <h1> ? ~All.h1 {background-color:#ffffff}
 Làm thế nào thay màu nền của chữ (text)? ~Color:
 Thuộc tính nào thay đổi kích cỡ chữ? ~Font-style
 Thuộc tính nào làm chữ trong tag p trở thành chữ đậm? ~P {font-weight:bold}
 Làm sao để hiển thị liên kết mà ko có gạch chân bên dưới? ~A {text-decoration:no underline}
 Làm sao để mỗi từ trong 1 dòng đều viết hoa ở đầu từ? ~Text-transform:uppercase
 Làm sao để thay đổi font của mỗi phần tử? ~F:
 Làm sao để tạo chữ đậm? ~Tất cả các câu trên đều đúng
 Làm thế nào để hiển thị viền 1 phần tử với kích thước đường viền như sau: ~Border-width:10px 1px 5px 20px
 Làm sao để thay đổi lề trái của một phần tử? ~Margin-left:
 Để định nghĩa khoảng trống giữa các cạnh (viền) của phần tử và nội dung, bạn sử dụng thuộc tính padding, có thể gán giá trị âm cho thuộc tính này không? ~undefined
 Làm thế nào để hình ở đầu mỗi dòng của 1 list (danh sách) có hình vuông? ~List-type: square
 Nút lệnh(button) nào.dưới đây cho phép gửi thông tin trên form tới server khi được click Chuột? ~Send
 Câu lệnh nào cho ... External Style sheet ta sử dụng thẻ: ~<LINK>
  Dreamweaver cho phép làm việc với mấy cách trình bày tài liệu: ~4
 Làm thế nào để bo tròn góc cạnh các phần tử? ~border-radius: 30px;
 Làm thế nào để thêm bóng (shadow) cho các phần tử trong CSS3? ~shadow-right: 10px shadow-bottom: 10px; 
 Làm thế nào để thêm hình ảnh xuất hiện trên đường viền cho các phần tử trong CSS3? ~border-variable: image url(image.png);
 Làm cách nào để thay đổi kích thước hình ảnh background bằng CSS3? ~background-proportion: 80px 60px;
  Làm cách nào để thêm bóng (shadow) cho đoạn text bằng CSS3? ~text-shadow: 5px 5px 5px grey;
 Làm thế nào để bắt buộc đoạn text xuống dòng? ~text-wrap: break-word;
 Đâu là lệnh chuyển đổi (transform) hợp lệ trong CSS3. ~matrix() 
 Làm thế nào để xoay phần tử trong CSS3? ~transform: rotate(30deg);
 Đâu là cách để chuyển đổi kích thước/tỷ lệ phần tử bằng CSS3? ~scale-object: 2,4;
 Làm cách nào để tạo hiệu ứng chuyển tiếp transition bằng CSS3? ~alpha-effect: transition (width,2s);
 Khẳng định sau đúng hay sai? Box-shadow được tất cả các trình duyệt hỗ trợ ~Sai
 Cần thêm tiền tố nào để các thuộc tính CSS3 hoạt động trên trình duyệt Mozilla Firefox cũ? ~-o- 
 Ba giá trị đầu tiên của text-shadow theo thứ tự là gì? ~vertical, blur, horizontal 
 4 giá trị của border-radius lần lượt là? ~bottom-left, bottom-right, top-right, top-left
 RGBa có nghĩa là gì? ~Red Green Blue alpha 
 Lệnh CSS có thể viết được ở đâu? ~Viết lẫn vào HTML nhưng khi viết có thẻ <Css> </Css> hoặc viết ra một file riêng và đặt tên có phần mở rộng là .css
 Khi sử dụng thẻ DIV có Class = thì ở phần khai báo CSS ta đặt tên đối tượng thế nào? ~ Đặt tên đối tượng có dấu # ở đầu tên đối tượng
 CSS dùng để làm gì? ~ Viết các ứng dụng sự kiện
 CSS chạy từ phía nào? ~ Người dùng tự chạy
 Biến trong javascript được khai báo thế nào? ~dim x=5 dim x
 Kiểu gán nào sau trong JavaScript không hợp lệ? ~ x *= y
  P {color:red;text-align:center;} ~ Thẻ P của HTML sẽ có màu đỏ căn lề giữa.
 Để nhúng mã JavaScript ...o tên thẻ ở thuộc tính style="" hoặc viết ra một file riêng và đặt tên có phần mở rộng là .css"
 Thẻ của HTML có cấu trúc thế nào? ~HTML tag là các từ khóa được bao quanh bởi dấu ngoặc nhọn như <%%>
 Khi lưu một tài liệu định dạng HTML phần mở rộng có thể là gì? ~ Phần mở rộng phải là html
 Thẻ trong HTML được viết thế nào? ~Được viết tùy thích và phải đúng nguyên tắc đóng mở theo chuẩn w3c
 Thuộc tính của các cặp thẻ trong HTML được quy định thế nào? ~Thuộc tính là một thẻ gốc đóng vai trò cung cấp thông tin, Thuộc tính được lưu trong giá trị các cặp tên / như: tên = "giá trị"
 Với các thẻ được quy định về cách thức hiện thị tài liệu, chúng ta có thể can thiệp để thay được được thêm các thông tin đó không? ~ Được thông qua việc khai báo lại thể style
 Ngôn ngữ kịch bản Javascript được viết theo: ~ Javascript
 Ngôn ngữ kịch bản Javascript và Java có giống nhau không? ~Có vì java cũng là ngôn ngữ lập trình web
 Ngôn ngữ kịch bản Javascript gần giống với ngôn ngữ nào sau đây? ~ Foxpro
 Khẳng định sau đúng hay sai? Box-shadow được tất cả các trình duyệt hỗ trợ ~Sai
 Cần thêm tiền tố nào để các thuộc tính CSS3 hoạt động trên trình duyệt Mozilla Firefox cũ? ~-o- 
 Ba giá trị đầu tiên của text-shadow theo thứ tự là gì? ~vertical, blur, horizontal 
 4 giá trị của border-radius lần lượt là? ~bottom-left, bottom-right, top-right, top-left
 RGBa có nghĩa là gì? ~Red Green Blue alpha 
 Lệnh CSS có thể viết được ở đâu? ~Viết lẫn vào HTML nhưng khi viết có thẻ <Css> </Css> hoặc viết ra một file riêng và đặt tên có phần mở rộng là .css
 Khi sử dụng thẻ DIV có Class = thì ở phần khai báo CSS ta đặt tên đối tượng thế nào? ~ Đặt tên đối tượng có dấu # ở đầu tên đối tượng
 CSS dùng để làm gì? ~ Viết các ứng dụng sự kiện
 CSS chạy từ phía nào? ~ Người dùng tự chạy
 Biến trong javascript được khai báo thế nào? ~dim x=5 dim x
 Kiểu gán nào sau trong JavaScript không hợp lệ? ~ x *= y
  P {color:red;text-align:center;} ~ Thẻ P của HTML sẽ có màu đỏ căn lề giữa.
 Để nhúng mã JavaScript ...o tên thẻ ở thuộc tính style="" hoặc viết ra một file riêng và đặt tên có phần mở rộng là .css"
 Thẻ của HTML có cấu trúc thế nào? ~HTML tag là các từ khóa được bao quanh bởi dấu ngoặc nhọn như <%%>
 Khi lưu một tài liệu định dạng HTML phần mở rộng có thể là gì? ~ Phần mở rộng phải là html
 Thẻ trong HTML được viết thế nào? ~Được viết tùy thích và phải đúng nguyên tắc đóng mở theo chuẩn w3c
 Thuộc tính của các cặp thẻ trong HTML được quy định thế nào? ~Thuộc tính là một thẻ gốc đóng vai trò cung cấp thông tin, Thuộc tính được lưu trong giá trị các cặp tên / như: tên = "giá trị"
 Với các thẻ được quy định về cách thức hiện thị tài liệu, chúng ta có thể can thiệp để thay được được thêm các thông tin đó không? ~ Được thông qua việc khai báo lại thể style
 Ngôn ngữ kịch bản Javascript được viết theo: ~ Javascript
 Ngôn ngữ kịch bản Javascript và Java có giống nhau không? ~Có vì java cũng là ngôn ngữ lập trình web
 Ngôn ngữ kịch bản Javascript gần giống với ngôn ngữ nào sau đây? ~ Foxpro
 Trình dịch PHP nào là trình dịch bạn cho là đúng? ~PHP Communicator
 Engine nào là nền tảng chính của PHP? ~ZENAT
  <?php
$x = array(
    1,
    3,
    2,
    3,
    7,
    8,
    9,
    7,
    3
);
$y = array_count_values($x);
echo $y[8];
?>
 ~8
 Khi thực thi PHP4 Script nó nhanh hơn PHP3 mấy lần ~Gấp 3
 Cài đặt Apache xong bạn có thể kiểm tra servername của bạn qua cách gọi ulr http://localhost, ngoài cách này còn cách nào khác nữa không? ~http://127.0.0.1
 Khi sử dụng bộ PHP & Apache bạn phải trả ~150$
 Ai là người đầu tiên phát minh ra PHP? ~James Gosling
 PHP dựa theo syntax của ngôn ngữ nào? ~VB Script
 W tượng trưng cho cụm từ nào trong gói WAMP? ~Whistler
 Truớc khi đổi thành PHP: Hypertext Preprocessor nguồn gốc của nó phát xuất từ cụm từ nào ~Personal HomePage Tools
 Khi thực thi biến này $Var3 = $var1 % $var2 dạng type của nó là ~integer
 Dạng type nào sẽ được tự động dịch khi gán biến này $var = 50.0 ~integer
 Số 41 này tương đương với số Bit nào ~10010010
 Ứng dụng PHP c...ửi mail trực tiếp từ script ~False
 Kết nối db nào là đúng ~mysql_open("localhost");
 Để +1 cho biến $count ~count++;
 Để ghi chú trong file php ~<!--…-->
 PHP có thể chạy bằng IIS trong windows ~True
 Trong PHP 5, MySQL được mặc định enable ~undefined
 Biến nào có tên sai ? ~undefined
 Trong PHP, die() và exit() là như nhau ~undefined
 Có cách nào submit form mà không cần button submit không? ~undefined
 Hàm nào sau đây dùng để lấy 1 hoặc nhiều giá trị ngẫu nhiên từ mảng php? ~Random_array()
  <?php
$x = array(
    "aaa",
    "ttt",
    "www",
    "ttt",
    "yyy",
    "tttt"
);
$y = array_count_values($x);
echo $y[ttt]; ~1
 Để nối 2 chuỗi trong php, sử dụng …? ~undefined
 Để chèn nội dung 1 tập tin vào, thì sử dụng ~include[]
 Hàm nào để ngưng script php chạy tiếp? ~quit()
 Hàm nào để in biến, chuỗi, số… ra ngoài trình duyệt trong php? ~undefined
  <?php
define("x", "5");
$x = x + 10;
echo x;
?> ~10
  <?php
$rest = substr("abcdef", -1);
$rest = substr("abcdef", 0, -1);
?> ~a,fedcb
  <?php
$str = "3dollars";
$a = 20;
$a += $str;
print ($a);
?> ~320 dollars
  <?php
function zz(&$x)
{
    $x = $x + 5;
}

$x = 10;
zz($x);
echo $x;
?> ~15
  <?php
$x = array(
    2 => "mouse",
    7 => "keyboard"
);
$y = array_keys($x);
echo $y[1];
?> ~7
  <?php
$data = "98.8degrees";
(double)$data;
(int)$data;
(string)$string;
echo $data;
?> ~98.8 degrees
  <?php
$x = "101.5degrees";
(double)$x;
(int)$x;
echo (string)$x;
?> ~101.5
 ) Để comment 1 dòng trong php, ngoài dấu // thì còn có dấu #, đúng hay sai? ~Sai
 Tên biến nào sau đây sai? ~$nic
  <?php
$father = "mother";
$mother = "son";
echo $$father;
?> ~mother
  <?php
$arr = array(
    5 => 1,
    12 => 2
);
$arr[] = 56;
$arr["x"] = 42;
echo var_dump($arr);
?> ~array(4) { [5]=>int(1) [12]=> int(2) [13]=> int(56) ["x"]=> int(42) }
  <?php
$date = "2009-5-19";
$time = "14:31:38";
$datetime = $date . $time;
echo date("Y-m-d:H:i:s", strtotime($datetime));
?> ~2009-5-19:14:31:38 
  <?php
$color = array(
    "red",
    "yellow",
    "w...RE
 Trong MySQL, Câu lệnh nào vừa xóa dữ liệu của bảng, vừa xóa luôn bảng ~DEL
 Các tập tin PHP có phần mở rộng mặc định là ~.xml
 Trình thông dịch trong PHP được gọi là? ~Không có câu nào đúng
 Ai là người tạo ra ngôn ngữ PHP? ~Rasmus Lerdorf
 PHP dựa theo cú pháp của ngôn ngữ nào? ~VB Script
 Kí tự W trong WAMP tượng trưng cho từ ~Whistler
 Khi thực thi: $var3 = $var1 % $var2, dạng type của $var3 là ~integer
 Dạng type nào sẽ được tự động dịch khi thực thi lệnh gán: $var = 50.0 ~boolean
 Để viết code PHP, bạn có thể sử dụng bộ Editor ~Notepad ++
 Phiên bản …. giới thiệu tính năng Try/catch Exception? ~PHP 5
 Câu lệnh nào lưu trữ giá trị 111 trong biến num? ~$num = 111;
  <?php
$num = 1;
$num1 = 2;
print $num . "+" . $num1;
?> ~Xảy ra lỗi
  <?php
$num = “1”;
$num1 = “2”;
print $num + $num1;
?> ~12
 Tên biến nào sau đây là hợp lệ ~$_hello
  <?php
$color = "maroon";
$var = $color[2];
echo "$var";
?> ~Error
  <?php
$score = 1234;
$scoreboard = (array)$score;
echo $scoreboard[0];
?> ~1
  <?php
$total = "25 students";
$more = 10;
$total = $total + $more;
echo "$total";
?> ~25 students
 Câu nào dưới đây có kết quả bằng với $add += $add ? ~$add = $add
 Câu lệnh nào output ra “$x” trên màn hình ~echo “$$x”;
  <?php
$a = "clue";
$a .= "get";
echo "$a";
?> ~get
  <?php
$a = 5;
$b = 5;
echo ($a === $b);
?> ~1
 Kí tự nào sau đây là kí tự xuống hàng trong PHP? ~/r
  <?php
$num = 10;
echo 'What is her age? \n She is $num years old';
?> ~What is her age? She is 10 years old
  <?php
$team = "arsenal";
switch ($team)
{
    case "manu":
        echo "I love man u";
    case "arsenal":
        echo "I love arsenal";
    case "manc":
        echo "I love manc";
}
?> ~I love arsenal
 Nếu $a = 12 thì câu lệnh sau: ($a == 12) ? 5 : 1 có kết quả là? ~1
  <?php
function calc($price, $tax = "")
{
    $total = $price + ($price * $tax);
    echo "$total";
}
calc(42);
?> ~Error
  <?php
$op2 = "blabla";
function foo($op1)
{
    echo $op1;
    echo $op2;
}
foo("hello");
?> ~hello
 Trong PHP, ...?php
function
case ()
{
    echo "Hello World! ";
    echo "Hello World! ";
    echo "Hello World! ";
}
case ();
?> ~Hello World! Hello World!
  <?php
function email()
{
    $email = ’user@yahoo . com’;
    $new = strstr($email, ‘@');
	echo $new;
}
email();
?> ~@yahoo.com
  <?php
$x = 0;
if ($x) print "hi";
else print "how are u";
?> ~hi
  <?php
$x = 0;
if ($x == 0) print "hi";
else print "how are u";
print "hello"
?> ~hi
  <?php
$x = 0;
if ($x == 1) if ($x >= 0) print "true";
else print "false";
?> ~error
  <?php
$a = 1;
if (echo $a) print "True";
else print "False";
?> ~Xảy ra lỗi
  <?php
$a = 1;
if (print $a) print "True";
else print "False";
?> ~False
 Tính đóng gói trong OOP là ~Polymorphism
 Tính đa hình trong OOP là ~Inheritance
 Tính kế thừa trong OOP là ~Polymorphism
 Từ khóa sau đây không được hỗ trợ bởi PHP ~public
 Dòng nào để khởi tạo một đối tượng thuộc lớp có tên foo trong PHP

  ~$obj = new foo ();
 Dòng nào để định nghĩa hằng số PI trong PHP ~constant PI = ‘3.1415’;
 Trong PHP, để gọi hằng số PI của lớp mathFunction ~echo mathFunction=PI;
 Trong PHP, dòng nào dưới đây để gọi một phương thức ~object::methodName();
 PHP không hỗ trợ từ khóa tầm vực nào sau đây: ~final
 Trong PHP, từ khóa tầm vực nào ngăn không cho một phương thức bị ghi đè bởi lớp con ~Abstract
 Điều nào là đúng về phương thức dựng (Constructors) trong PHP: ~ii), iii) và iv) đúng
 Khai báo nào là đúng cho phương thức dựng (Constructor) trong PHP ~_construct()
 …. Giới thiệu từ khóa “instanceof” ? Điền vào dấu ... ~PHP 6
 Trong PHP, hàm nào sau đây được dùng để xác định một lớp có tồn tại hay không? ~class_exist()
 Hàm nào sau đây được dùng để xác định loại của đối tượng object trong PHP ~is_obj()
 Tính trừu tượng trong OOP là ~Abstraction
 Tính trừu tượng trong OOP là ~Encapsulation
 Tính trừu tượng trong OOP là ~Inheritance
  <?php
class Example
{
    public $name;
    function Sample()
    {
        echo "This is an example";
    }
}
?> ~class Example
 Trong PHP, từ khóa nào được...?php
session_start();
if (!array_key_exists('counter', $_SESSION))
{
    $_SESSION['counter'] = 0;
}
else
{
    $_SESSION['counter']++;
}
session_regenerate_id();
echo $_SESSION['counter'];
?> ~1
 Trong các cách sau đây, cách nào giúp bạn nhận được dữ liệu gửi đi từ Form mà không chắc chắn cơ chế nó được gửi đi như thế nào? ~$_POST[]
 Phát biểu nào sao đây sai về XML ~Các thẻ XML khá giống với HTML: tag, data, attribute
 Phát biểu nào sao đây đúng về công dụng của XML? ~Lưu trữ thông tin nhỏ
 Trong Xpath để lấy ra thông tin phần tử ở vị trí cuối cùng, chúng ta sử dụng ~end()-1
 Phương thức nào sau đây được sử dụng để lấy các không gian tên được sử dụng trong một tài liệu XML từ một đối tượng SimpleXMLElement? ~getNamespaces()
 Phương thức nào sau đây được sử dụng để lấy tên của phần tử XML từ một đối tượng SimpleXMLElement? ~getName()
 Trong DomDocument sử dụng phương thức nào để thêm node vào một node nào đó cho trước ~insertFirst
 Lập trình hướng đối tượng có tên vắt tắt là ? ~OPO
 Phát biểu nào sao đây SAI về phương thức __construct() ~__construct() được đặt tên trùng với tên class
 Trong các điều khiển truy cập sau đây, điều khiển nào xác định rằng một tính năng có thể được truy cập bởi tất cả các lớp khác? ~Abstract
  <?php
class A
{
    static $word = "hello";
    static function hello()
    {
        print static ::$word;
    }
}
class B extends A
{
    static $word = "bye";
}
B::hello();
?> ~hello
 Làm sao để truy cập vào một thuộc tính trong class thông qua $this ~$this->varname
  <?php
class A
{
};
class B1 extends A
{
};
class_alias('A', 'B2');
$b1 = new B1;
echo get_class($b1);
$b2 = new B2;
echo get_class($b2);
?> ~B1B2
  <?php
class number
{
    public $a = 10;
    public $b = 20;
    private $c = 30;
}
$numbers = new number();
foreach ($numbers as $var => $value)
{
    echo "$value ";
}
?> ~10 20 30
  <?php
interface foo
{
}
class_alias('foo', 'bar');
echo interface_exists('bar') ? 'yes' : 'no';
?> ~Error
 Phương pháp nào sau đây được gọi để in trự...e
 Tên 1 database tối đa có bao nhiêu kí tự ~72
 Câu lệnh nào để tạo ra database “student” ~DATABASE /student
 Trong MySQL, Câu lệnh nào chỉ xóa dữ liệu của bảng ~DROP
 Trong SQL, từ khóa nào được dùng để sắp xếp kết quả trả về? ~SORT BY
 PHP hỗ trợ hệ Quản Trị CSDL nào sớm nhất?  ~MySQL
 Câu lệnh nào được sử dụng để tạo bảng?  ~CREATE TABLE table_name (column_name column_type);
 Câu lệnh nào được sử dụng để chọn CSDL?  ~mysqli=select_db(‘databasename’);
 Trong lớp mysqli của PHP, phương thức nào dùng để gửi câu truy vấn? ~query()
 Trong PHP, hàm nào dùng để nhận dữ liệu trả về từ câu truy vấn database? ~get_array() and get_column()
  $a = array(
 null => 'a',
 true => 'b',
 false => 'c',
 0 => 'd',
 1 => 'e',
 '' => 'f'
);
echo count($a); ~5
  $a = 1;
{
 $a = 2;
}
echo $a; ~1
  $var = 'a';
$VAR = 'b';
echo "$var$VAR"; ~AB
  <?php
$RESULT = 11 + 11 + 0x11;
echo "$RESULT";
?> ~39
  $a = array();
if ($a == null) { 
 echo 'true';
} else {
 echo 'false';
} ~Không in
  $var = 1 / 2; ~0,5
